# This is where I'm gonna put Twitter documentation because apparently Twitter can't keep their shit together.

from dotenv import load_dotenv
import tweepy

load_dotenv()
key = os.getenv("API_KEY")
secret = os.getenv("API_KEY_SECRET")